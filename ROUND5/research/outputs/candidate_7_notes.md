# Candidate 7 Notes

Aggressive thesis: combine the high-upside `PEBBLES_XL` leg with a gated `PEBBLES_L` overlay, because both paid on the official window and Candidate 1 showed the category relation is real.

Build source: aggressive repair of Candidates 1 and 9.

Products traded: `PEBBLES_XL`, `PEBBLES_L`.

Why more aggressive than 1-5: concentrates in two PEBBLES products, uses larger order sizes, crosses when the signal is strong, and does not wait for tiny passive fills.

Position-limit use: `PEBBLES_XL` can hit full +/-10; `PEBBLES_L` is lower weight but still allowed to reach large inventory under strong evidence.

Why not hardcoded/overfit: rolling z-score/volatility/imbalance only; no official timestamps, prices, or fixed day logic.

Expected upside: higher official-window PnL than Candidate 6 if `PEBBLES_L` continues to transfer.

Expected failure mode: `PEBBLES_L` was poor on full history, so this branch is explicitly high-variance.

Promotable if: official result confirms the `PEBBLES_L` overlay adds more than it costs versus Candidate 6.
