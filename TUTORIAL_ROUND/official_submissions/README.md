Drop new official submission bundles here if you want them separated from the legacy numeric folders in the round root.

The tooling supports both:

- legacy bundles directly under `TUTORIAL_ROUND\` named like `21063\`
- managed bundles under `TUTORIAL_ROUND\official_submissions\`
